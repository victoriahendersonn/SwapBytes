/**
 * The mDNS protocol for local network peer discovery, using libp2p. The rendezvous server
 * is use for broader discovery.
 * 
 * mDNS will broadcast a request on the network and will be replied to by other nodes
 * with their multiaddresses, note that mDNS only works on local networks.
 */



 